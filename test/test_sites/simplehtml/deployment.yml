# deployment settings for the 'simplehtml' test site
global:
  filterset: full

html:
  filterset:
    - whitespace
    - beautifytext

css:
  filterset:
    - minify

png:
  filterset:
    # try very hard to get smaller PNGs:
    - bruteforce